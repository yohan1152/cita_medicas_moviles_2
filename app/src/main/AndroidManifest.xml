<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.citasmedicas">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SeleccionarEspecialidadActivity"></activity>
        <activity android:name=".CrearCitasActivity" />
        <activity android:name=".HistorialCitasActivity" />
        <activity android:name=".AsignarCitaActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".EspecialidadesActivity" />
        <activity android:name=".EstadosActivity" />
        <activity android:name=".RolesActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".RegistrarUsuario" />
        <activity
            android:name=".PrincipalAdministradores"
            android:label="@string/title_activity_principal_administradores"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PrincipalMedicos"
            android:label="@string/title_activity_principal_medicos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PrincipalPacientes"
            android:label="@string/title_activity_principal_pacientes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>